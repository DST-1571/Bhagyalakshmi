<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sourceedge.bhagyalakshmi.orders">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@drawable/bhagyalaksmi"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".login.Login"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".dashboard.controller.Dashboard"
            android:label="@string/title_activity_dashboard"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".distributorsales.controller.DistributorSales"
            android:label="@string/title_activity_distributor_sales"
            android:theme="@style/AppTheme" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".location.controller.Location"
            android:label="@string/title_activity_location"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".orders.controller.AdminOrders"
            android:label="@string/title_activity_admin_orders"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".distributorsales.controller.RetailerLookUp"
            android:label="@string/title_activity_retailer_look_up"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".orders.controller.DistributorSalesOrders"
            android:label="@string/title_activity_distributor_sales_orders"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".orders.controller.DistributorOrders"
            android:label="@string/title_activity_distributor_orders"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".orders.controller.SalesPersonOrders"
            android:label="@string/title_activity_sales_person_orders"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".salesperson.controller.SalesPersonLookUp"
            android:label="@string/title_activity_sales_person_orders"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".orders.controller.OrderSuccess"
            android:label="@string/title_activity_order_success"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>